# BRIEF-chunk10.md — The ladder (2026-08-15)
# Plan chunk 10 + amendment v1.1 are the spine. This file governs.

## Part 0
0a. Chunk-9 fold-ins land first: golden asserts pool composition growth;
    episode loader refuses frozen instruments (meta role tag, both
    polarities); one-line confirmation that no shakedown-trained weights
    persist anywhere.

## Design (decided; deviations via BLOCKED)
1. CURRENCY RULING: z-vs-par is the scoring currency ONLY for
   rule-denominated arms (our model, RandomRewriter, GreedyHeuristic —
   they act in our rules, their steps are real steps). SympySolver rungs
   score paired solve-vs-budget, with the currency stated on every row —
   two currencies never silently mix, and a sympy row can never be read
   as a z row. Sympy derivations are never par, per spec.
2. Every opponent family gets its determinism probe as a construction
   gate (inherited law, the skill-limiter lesson): GreedyHeuristic
   probed deterministic; sympy probed + its version pinned in row
   provenance (the CAS version is part of the rung's identity), context-
   managed, clean-skip if absent; RandomRewriter is stochastic BY DESIGN
   — per-problem derived seeds, reps config, and the nondeterministic
   flag on its rows from day one, not after a surprise.
3. Paired problem sets: frozen at birth, digested into ANCHORS, and
   contamination-censused against everything — problem-level AND
   state-level (L6; the F-08/F-09 machinery points at them).
4. pair_scores persisted from row one; the paired-difference bootstrap
   implemented AND used for any pass-vs-pass claim — the test of record.
5. Self-match null, race form: the model against itself is the same
   model run twice on the paired set under the eval profile => the two z
   vectors are IDENTICAL => every paired difference exactly 0. The
   no-hidden-state detector, with its contrast case (self-play profile
   breaks the identity — proving it non-vacuous).
6. Four-tuples on every ladder gate (floor, null, threshold, measured).
   role field, calibration_note, saturated-CI rendering, watchlist with
   live verdicts + baseline_asof — all ported as law, not adopted
   incident-by-incident.
7. Ladder resumable mid-pass; waiters reference completion markers,
   never process patterns.

## DONE-WHEN (per plan; provisional thresholds revise only via BLOCKED)
- Synthetic Elo tests green incl. the rigged-50% null.
- Self-match identity EXACT, contrast case beside it.
- One full smoke pass against all rungs, every number verbatim, wall-
  clock split included.
- Resume mid-pass proven at a kill point.
Three failed attempts at any gate → BLOCKED, never a weakened gate.
