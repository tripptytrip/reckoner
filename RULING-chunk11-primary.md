# RULING-chunk11-primary.md — principal, 2026-08-15

Committed **verbatim on receipt, before acting on it**, per standing law.

---

Chunk 10 is closed, and two of its closing items deserve permanent marks. F-18's
disposition was verified rather than asserted — the post-freeze re-census at 0/0
is the digest proving it's the post-removal digest — and the census raising with
F-18's own numbers quoted at the failure point is a refusal that teaches. And
F-16's arc is the helpers-prevent ladder climbed to its top rung in one week: rule
("compose after") → repeated violation → mechanism (pytest_sessionfinish writes
the number, so it cannot be authored), with the whole_suite flag anticipating the
partial-run-reads-as-full corner before anyone hit it. "A rule of composition
order loses to ergonomics, so the number stopped being authored" is the whole
philosophy of this project in one sentence.

F-20 is accepted, generalization and all, and Part 0 did precisely the job it
existed for: the succession lesson arrived by arithmetic, pre-launch, instead of
by surprise at pass 15. Your diagnosis of the branch rule's blind spot is kept
verbatim — the fallback's headroom was never measured because it didn't exist when
the rule was written — and the general form joins rider (c): branch rules need
four-tuples on every branch, including the ones they fall back to. A pre-registered
rule that can only interrogate one of its own arms is half a rule.

The ruling — the primary is search economy. When absolute performance saturates
the scale, the measurable quantity is efficiency, and here that isn't a
consolation prize: the policy and value amortizing search into the net is the
AlphaZero effect — the loop's textbook product, and this domain reached it at
iteration zero on the exact-par scale. So:

P1: at-par rate on the frozen suites at sims = s\*, paired against the Phase-1
anchor at the same s\*, paired-difference bootstrap as the test of record, ~20
iterations at --ladder-every 5. The movable mass is the −1 → 0 transition at low
sims, where −1 is plentiful — z-denominated, currency intact, invariants
untouched. And s\* is chosen by a rule frozen before the sweep runs, so no post-hoc
selection exists: Part-0b sweeps the anchor's at-par rate over sims ∈ {6, 8, 12,
16, 48} on the suites, and s\* = the sims whose anchor rate lands nearest 0.55,
within [0.4, 0.7] — mid-scale, maximal headroom and sensitivity. The primary's
four-tuple: floor = uniform-stub at s\*, null = the anchor's own paired baseline,
ceiling = 1.0 with the distance stated. Co-benefit, not motive: the primary rung
is ~8× cheaper than the 48-sim pass, which turns your 8.3 eval-hours into
something a 20-iteration campaign can afford. No-regress rides at the top of the
budget: sims=48 at-par must hold the anchor's 1193/1200 band — the loop may not
buy low-budget competence by selling high-budget competence — plus gates 10b and
11 on the ruled wiring, as planned.

Second instrument, minted before the freeze: the mid-strata suites. solve_in_7..10,
200 each, scripted par — which is a provisional floor and therefore beatable: the
+1 cell is live there, the full z scale breathes, and the par game's own escalation
architecture finally has a frozen instrument denominated where racing is possible.
Part-0c mints them (generator extended to deeper emission, scripted-par labels with
their provenance, censused at both levels, digested into ANCHORS), their trajectory
reports every ladder pass, and — the chess lesson written in before it's needed —
succession is pre-registered: if the economy primary saturates, the mid-strata
suite is M2's primary, declared now, in PREREG-m1's own text. The rungs go
informational at matched s\* (greedy and sympy against the model-at-s\*, both
currencies in their lanes), and the rest of the prereg carries what's long been
enumerated: the funnel signature named, the switch criterion's trajectory with
abstention reasons as data, pool composition growth, z-by-par_source as the
draw-inflation watch, the BLOCKED branches.

Order of operations: freeze the s\* selection rule and the Part-0c census/freeze
protocol first — rule before measurement, all the way down — then run Part-0b's
sweep and mint Part-0c, then PREREG-m1 freezes on their numbers with the primary's
four-tuple filled in by mechanism rather than choice. The brief's final text lands
with those numbers in it. Run the sweep.
