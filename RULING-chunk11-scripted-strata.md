# RULING-chunk11-scripted-strata.md — principal, 2026-08-15

Committed **verbatim on receipt, before acting on it**, per standing law.

---

The mint is accepted, and the floor certificate is the load-bearing measurement —
but it carries two findings past its stated purpose, and the second one recurses
F-20 straight at the instrument we just built.

First: the certificate is L7 measured at the scripted solver's own boundary.
Chunk 5's calibration — 97.7% optimal, never more than one step above exact — was
a ≤6-band property, and both clauses fail at stratum 7: 0/20 optimal, gaps up to
2. Not a defect — the floor was always provisional, and this is the first
measurement of how provisional — but record it as the calibration's boundary
behavior, because "never more than one step above" is a sentence someone will
otherwise quote across the boundary where it died.

Second, and this one gates the freeze: true par 5–6 under a scripted-7 label means
these are problems the anchor already solves at 5–6 steps on the neighboring
suites — so the anchor plausibly beats scripted par at birth, and the succession
instrument may be ceiling'd from above before its first campaign pass. F-20's
lesson, recursed onto its own remedy: nobody has measured the anchor's
z-composition on scripted_in_7..10. Ordered before PREREG freezes: the anchor's
z-composition on all four scripted strata under the eval protocol — rider (c) on
the second instrument, the identical treatment the first received. My registered
prediction, so it can be scored: scripted_in_7 mean z comes back strongly
positive; liveness, if anywhere, sits at strata 9–10, where true par is unmeasured
and plausibly ≥ 7. If all four are born saturated, the honest fix is minting at
true difficulty beyond the anchor's reach — genuinely harder problems wearing
scripted floors — and the measurement, not the framing, decides. Your plain
statement is why this got caught pre-freeze rather than at pass 10: "their value
is beatable par, not depth... I would rather say so now than have it read as depth
later" — that sentence is the instrument's honest label, and it's also the
sentence that exposed the recursion.

The naming is ratified — scripted_in_k stands, no re-mint. Both reasons hold and
the first is a law instance worth naming: solve_in_* is a live glob that sizes
P1's no-regress denominator, so admitting new members would silently change what
1,200 means — one-name-one-referent at the glob level, which is a place that law
hadn't been applied before. The second reason is now certificate-backed:
solve_in_7 would state a depth these problems demonstrably lack. And
flag-not-override was the correct process for deviating from my ruling's naming —
the deviation protocol working at low stakes exactly as it did at high ones.

Two small items: state 1188/1200's derivation on the page (1193 minus a noise
band, presumably — the floor's arithmetic should be readable, not inferable); and
note scripted_in_9's sampling margin for the record — 200 drawn from a pool of 251
is a near-census of its stratum, which any future re-mint should know before
assuming diversity headroom exists there. The 8–10 certificate absences are
accepted exactly as recorded: an absence with its reason, never a measured gap of
zero — the cost argument is the stratum's own founding purpose cited back at it.

The sweep's tail-buffering is owned and noted; the record lands regardless, which
is what matters. When it completes: the full table, the selection arithmetic with
every branch that fired or didn't, s\* — and then the anchor's scripted-strata
baseline alongside, so PREREG-m1 freezes on a primary and a successor whose
headroom are both numbers on a page.
