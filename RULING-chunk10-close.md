# RULING-chunk10-close.md — principal, 2026-08-15

Committed **verbatim on receipt, before acting on it**, per standing law.

---

The ladder audit closes: 251 + 138 = 389, the SIGKILL was real rather than
simulated (an exception would never have touched the repair path — the right
reason to use the real signal), and 778 identical row-for-row with zero
duplicates is the resume contract holding under actual violence. F-19 is the
catch of the chunk and its fix is the strongest available form — removing the
caller's choice makes the currency error inexpressible, and your mechanism
statement is exact: both denominations land in {−1, 0, +1}, both pair, both
bootstrap, both give zero under determinism, so nothing downstream could have
caught it — only the contrast case's near-vacuity (1/389, "a hair from vacuous")
carried the signal, which is the contrast-case law justifying itself again. F-17
names a new micro-species for the taxonomy: a test that names the law and checks
something weaker — true under both the lawful and unlawful implementations, which
is camouflage, not coverage. F-16's method rule is accepted with its diagnosis
kept: a number authored before its measurement exists is a prediction wearing a
report's clothes.

F-18 needs its disposition stated, and chunk 10 closes on it. Eleven state-level
hits at 2.8% of the paired set — the menu is the two precedents: removed pre-freeze
(preferred if f0b10fd1 is the post-removal digest; a measurement set should be
born clean when cleaning costs eleven rows), or frozen-with-annotation, in which
case the F-09 pattern applies and every pass reports both numbers. Say which, and
confirm the structural completion: the both-level census is now the only census —
single-level calls removed or impossible — because "true and wrong about the only
level doing work" is exactly the sentence that should never be sayable again.

And before any PREREG text gets drafted, a catch on the plan itself: the chunk-11
primary as written — "CI-separated improvement in beat-par rate on the frozen
suites" — measures the impossible cell. The suites carry BFS-exact par; nothing
beats exact par; our own invariant refuses rows claiming otherwise. The primary's
numerator is structurally zero for all time. And the natural fallback — at-par
rate — may be born saturated: the anchor already solves 1,200/1,200 with median
steps equal to par, so the only movable mass is the over-par tail, whose size
nobody has measured. This is rider (c) applied to the campaign's own primary
criterion: a threshold nobody computed the floor or ceiling of is not a gate, and
that goes double for the criterion the whole campaign answers to.

So chunk 11 opens with Part 0, and the brief follows its numbers:

Measure the anchor's suite z-composition — the full steps-minus-par histogram
across all six suites, under M1's exact eval protocol (sims, m, budget, eval
profile, value-silent wiring) — so the over-par tail's size is a number, not a
hope.
Measure the rung baselines: the anchor against greedy and sympy on the frozen
paired set, pair_scores from row one, both currencies in their lanes.
Then the primary is chosen against measured headroom and four-tupled: if the
suite tail is big enough to carry CI separation over ~20 iterations, the primary
is tail-reduction (paired, bootstrap as test of record); if it's thin, the primary
moves to the rung trajectory — the chess succession lesson (anchor rung saturating
while skill-0 resolved +429) arriving before this campaign instead of during it,
by arithmetic instead of by surprise.

PREREG-m1 freezes after that, carrying: the chosen primary with its four-tuple;
no-regress on gates 10b and 11 as re-declared on the ruled wiring; the entropy
funnel signature named (which H falls, on which population, by how much); the
switch criterion's expected trajectory with abstention reasons as data and the
cross-switch watch armed; pool composition growth; z-by-par_source as the
draw-inflation watch; the BLOCKED branches. A primary whose floor and ceiling
nobody computed doesn't freeze — that's the whole record speaking with one voice.
Run Part 0; the brief lands on its numbers.
