# 50 derivations — the chunk 4 manual gate

*Generated by `make docs` from `scripts/render_derivations.py`. Do not hand-edit —
`tests/test_interpreter.py::test_derivations_md_is_current` regenerates and compares.*

**What the proofread certifies:** that a reader can follow every line without the
code; that the rule named matches the transformation shown, every time; that
base-625 numerals — multi-digit and negative especially — render correctly; and
that nothing was prettified into unfaithfulness.

**Expect `21 + (−6)` on the page.** That is not a bug; it is the state.
`sub_both_sides` moves an addend across as its negation, and no `SUB` node exists
there. Rendering it as `21 − 6` would read back as a `SUB` node — a different
state — which is why `read_expr(render_expr(e)) == e` is a test and not a wish.

## Coverage manifest

A gate reports what it covered, not only that it passed.

| stratum | count |
|---|---|
| derivations | 50 |
| rule `add_both_sides` | 3 |
| rule `combine_like_terms` | 18 |
| rule `div_both_sides` | 22 |
| rule `eval_add` | 33 |
| rule `eval_mul` | 5 |
| rule `eval_sub` | 3 |
| rule `sub_both_sides` | 21 |
| goal EVALUATE | 14 |
| goal SIMPLIFY | 14 |
| goal SOLVE | 22 |
| with multi-digit base-625 numerals | 10 |
| with negative numerals | 33 |
| x on both sides | 3 |
| derivation lengths | {1: 29, 2: 2, 3: 12, 4: 1, 5: 4, 6: 2} |

`add_both_sides` is reachability-redundant (`REGISTERED-ROUNDS.md` ROUND-01), so it
never appears in an optimal derivation. The entries that exercise it are labelled
ILLUSTRATIVE — an unlabelled suboptimal derivation in a proofread set is a
misleading exhibit.

---

### 01. par 1 — one division

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    1  (par_source=bfs)
start  3x = 15

3x = 15  ──[rule 6 div_both_sides @ site 2 (3)]──►  x = 5

result x = 5
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `10 1 8 1 4 1 35 2 11 2 4 1 47 2 2`</sub>

### 02. par 1 — negative coefficient

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    1  (par_source=bfs)
start  −4x = 20

−4x = 20  ──[rule 6 div_both_sides @ site 2 (−4)]──►  x = −5

result x = −5
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `10 1 8 1 5 1 36 2 11 2 4 1 52 2 2`</sub>

### 03. par 3 — the spec's own example

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  3x + 6 = 21

3x + 6 = 21     ──[rule 5 sub_both_sides @ site 5 (6)]──►  3x = 21 + (−6)
3x = 21 + (−6)  ──[rule 0 eval_add @ site 4 (21 + (−6))]──►  3x = 15
3x = 15         ──[rule 6 div_both_sides @ site 2 (3)]──►  x = 5

result x = 5
       solved in 3 step(s), par 3, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 4 1 35 2 11 2 4 1 38 2 2 4 1 53 2 2`</sub>

### 04. par 3 — negative constant

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  2x + (−4) = 10

2x + (−4) = 10  ──[rule 5 sub_both_sides @ site 5 (−4)]──►  2x = 4 + 10
2x = 4 + 10     ──[rule 0 eval_add @ site 4 (4 + 10)]──►  2x = 14
2x = 14         ──[rule 6 div_both_sides @ site 2 (2)]──►  x = 7

result x = 7
       solved in 3 step(s), par 3, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 4 1 34 2 11 2 5 1 36 2 2 4 1 42 2 2`</sub>

### 05. par 3 — negative answer

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  5x + 3 = −17

5x + 3 = −17     ──[rule 5 sub_both_sides @ site 5 (3)]──►  5x = −3 + (−17)
5x = −3 + (−17)  ──[rule 0 eval_add @ site 4 (−3 + (−17))]──►  5x = −20
5x = −20         ──[rule 6 div_both_sides @ site 2 (5)]──►  x = −4

result x = −4
       solved in 3 step(s), par 3, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 4 1 37 2 11 2 4 1 35 2 2 5 1 49 2 2`</sub>

### 06. par 3 — coefficient 1 vanishes

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  1x + 9 = 21

1x + 9 = 21     ──[rule 5 sub_both_sides @ site 5 (9)]──►  1x = 21 + (−9)
1x = 21 + (−9)  ──[rule 0 eval_add @ site 4 (21 + (−9))]──►  1x = 12
1x = 12         ──[rule 6 div_both_sides @ site 2 (1)]──►  x = 12

result x = 12
       solved in 3 step(s), par 3, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 4 1 33 2 11 2 4 1 41 2 2 4 1 53 2 2`</sub>

### 07. multi-digit — three-figure constant

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  3x + 700 = 2200

3x + 700 = 2200     ──[rule 5 sub_both_sides @ site 5 (700)]──►  3x = 2200 + (−700)
3x = 2200 + (−700)  ──[rule 0 eval_add @ site 4 (2200 + (−700))]──►  3x = 1500
3x = 1500           ──[rule 6 div_both_sides @ site 2 (3)]──►  x = 500

result x = 500
       solved in 3 step(s), par 3, z = 0
```

base-625 panel for the largest numeral in this derivation:

```
glyph convention: reckoner-local placeholder, NOT base-625-canonical (vendoring registered as ROUND-03)
2200 = D3 D325
┌───┐ ┌───┐
│0 0│ │2 3│
│0 3│ │0 0│
└───┘ └───┘
```

<sub>start tokens: `10 1 6 1 8 1 4 1 35 2 11 2 4 1 33 107 2 2 4 1 35 357 2 2`</sub>

### 08. multi-digit — four-figure right side

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    1  (par_source=bfs)
start  7x = 8750

7x = 8750  ──[rule 6 div_both_sides @ site 2 (7)]──►  x = 1250

result x = 1250
       solved in 1 step(s), par 1, z = 0
```

base-625 panel for the largest numeral in this derivation:

```
glyph convention: reckoner-local placeholder, NOT base-625-canonical (vendoring registered as ROUND-03)
8750 = D14 D0
┌───┐ ┌───┐
│0 0│ │0 0│
│2 4│ │0 0│
└───┘ └───┘
```

<sub>start tokens: `10 1 8 1 4 1 39 2 11 2 4 1 46 32 2 2`</sub>

### 09. multi-digit negative constant

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  4x + (−1250) = 2250

4x + (−1250) = 2250  ──[rule 5 sub_both_sides @ site 5 (−1250)]──►  4x = 1250 + 2250
4x = 1250 + 2250     ──[rule 0 eval_add @ site 4 (1250 + 2250)]──►  4x = 3500
4x = 3500            ──[rule 6 div_both_sides @ site 2 (4)]──►  x = 875

result x = 875
       solved in 3 step(s), par 3, z = 0
```

base-625 panel for the largest numeral in this derivation:

```
glyph convention: reckoner-local placeholder, NOT base-625-canonical (vendoring registered as ROUND-03)
2250 = D3 D375
┌───┐ ┌───┐
│0 0│ │3 0│
│0 3│ │0 0│
└───┘ └───┘
```

<sub>start tokens: `10 1 6 1 8 1 4 1 36 2 11 2 5 1 34 32 2 2 4 1 35 407 2 2`</sub>

### 10. three base-625 digits in one numeral

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    1  (par_source=bfs)
start  2x = 781250

2x = 781250  ──[rule 6 div_both_sides @ site 2 (2)]──►  x = 390625

result x = 390625
       solved in 1 step(s), par 1, z = 0
```

base-625 panel for the largest numeral in this derivation:

```
glyph convention: reckoner-local placeholder, NOT base-625-canonical (vendoring registered as ROUND-03)
781250 = D2 D0 D0
┌───┐ ┌───┐ ┌───┐
│0 0│ │0 0│ │0 0│
│0 2│ │0 0│ │0 0│
└───┘ └───┘ └───┘
```

<sub>start tokens: `10 1 8 1 4 1 34 2 11 2 4 1 34 32 32 2 2`</sub>

### 11. x on both sides

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    5  (par_source=bfs)
start  2x + 18 = 5x + 3

2x + 18 = 5x + 3     ──[rule 5 sub_both_sides @ site 2 (2x)]──►  5x + (−2x) + 3 = 18
5x + (−2x) + 3 = 18  ──[rule 3 combine_like_terms @ site 1 (5x + (−2x) + 3)]──►  3x + 3 = 18
3x + 3 = 18          ──[rule 5 sub_both_sides @ site 5 (3)]──►  3x = 18 + (−3)
3x = 18 + (−3)       ──[rule 0 eval_add @ site 4 (18 + (−3))]──►  3x = 15
3x = 15              ──[rule 6 div_both_sides @ site 2 (3)]──►  x = 5

result x = 5
       solved in 5 step(s), par 5, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 4 1 34 2 11 2 4 1 50 2 2 6 1 8 1 4 1 37 2 11 2 4 1 35 2 2 2`</sub>

### 12. x on both sides, negative constant

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    5  (par_source=bfs)
start  3x + 14 = 7x + (−2)

3x + 14 = 7x + (−2)     ──[rule 5 sub_both_sides @ site 2 (3x)]──►  7x + (−3x) + (−2) = 14
7x + (−3x) + (−2) = 14  ──[rule 3 combine_like_terms @ site 1 (7x + (−3x) + (−2))]──►  4x + (−2) = 14
4x + (−2) = 14          ──[rule 5 sub_both_sides @ site 5 (−2)]──►  4x = 2 + 14
4x = 2 + 14             ──[rule 0 eval_add @ site 4 (2 + 14)]──►  4x = 16
4x = 16                 ──[rule 6 div_both_sides @ site 2 (4)]──►  x = 4

result x = 4
       solved in 5 step(s), par 5, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 4 1 35 2 11 2 4 1 46 2 2 6 1 8 1 4 1 39 2 11 2 5 1 34 2 2 2`</sub>

### 13. x on both sides, negative answer

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    5  (par_source=bfs)
start  4x + 6 = 9x + 31

4x + 6 = 9x + 31     ──[rule 5 sub_both_sides @ site 2 (4x)]──►  9x + (−4x) + 31 = 6
9x + (−4x) + 31 = 6  ──[rule 3 combine_like_terms @ site 1 (9x + (−4x) + 31)]──►  5x + 31 = 6
5x + 31 = 6          ──[rule 5 sub_both_sides @ site 5 (31)]──►  5x = 6 + (−31)
5x = 6 + (−31)       ──[rule 0 eval_add @ site 4 (6 + (−31))]──►  5x = −25
5x = −25             ──[rule 6 div_both_sides @ site 2 (5)]──►  x = −5

result x = −5
       solved in 5 step(s), par 5, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 4 1 36 2 11 2 4 1 38 2 2 6 1 8 1 4 1 41 2 11 2 4 1 63 2 2 2`</sub>

### 14. evaluate — a sum

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    1  (par_source=bfs)
start  17 + (−25)

17 + (−25)  ──[rule 0 eval_add @ site 0 (17 + (−25))]──►  −8

result −8
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `6 1 4 1 49 2 5 1 57 2 2`</sub>

### 15. evaluate — a numeric SUB node

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    1  (par_source=bfs)
start  21 − 6

21 − 6  ──[rule 1 eval_sub @ site 0 (21 − 6)]──►  15

result 15
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `7 1 4 1 53 2 4 1 38 2 2`</sub>

### 16. evaluate — SUB inside a sum

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    2  (par_source=bfs)
start  −5 + (40 − 15)

−5 + (40 − 15)  ──[rule 1 eval_sub @ site 2 (40 − 15)]──►  25 + (−5)
25 + (−5)       ──[rule 0 eval_add @ site 0 (25 + (−5))]──►  20

result 20
       solved in 2 step(s), par 2, z = 0
```

<sub>start tokens: `6 1 5 1 37 2 7 1 4 1 72 2 4 1 47 2 2 2`</sub>

### 17. evaluate — a product

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    1  (par_source=bfs)
start  25 × 25

25 × 25  ──[rule 2 eval_mul @ site 0 (25 × 25)]──►  625

result 625
       solved in 1 step(s), par 1, z = 0
```

base-625 panel for the largest numeral in this derivation:

```
glyph convention: reckoner-local placeholder, NOT base-625-canonical (vendoring registered as ROUND-03)
625 = D1 D0
┌───┐ ┌───┐
│0 0│ │0 0│
│0 1│ │0 0│
└───┘ └───┘
```

<sub>start tokens: `8 1 4 1 57 2 4 1 57 2 2`</sub>

### 18. evaluate — multi-digit product

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    1  (par_source=bfs)
start  625 × 3

625 × 3  ──[rule 2 eval_mul @ site 0 (625 × 3)]──►  1875

result 1875
       solved in 1 step(s), par 1, z = 0
```

base-625 panel for the largest numeral in this derivation:

```
glyph convention: reckoner-local placeholder, NOT base-625-canonical (vendoring registered as ROUND-03)
625 = D1 D0
┌───┐ ┌───┐
│0 0│ │0 0│
│0 1│ │0 0│
└───┘ └───┘
```

<sub>start tokens: `8 1 4 1 33 32 2 4 1 35 2 2`</sub>

### 19. evaluate — mixed, three steps

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    3  (par_source=bfs)
start  7 + (9 − 30) + 12 × 12

7 + (9 − 30) + 12 × 12  ──[rule 1 eval_sub @ site 2 (9 − 30)]──►  7 + (−21) + 12 × 12
7 + (−21) + 12 × 12     ──[rule 0 eval_add @ site 0 (7 + (−21) + 12 × 12)]──►  −14 + 12 × 12
−14 + 12 × 12           ──[rule 2 eval_mul @ site 2 (12 × 12)]──►  144 + (−14)
144 + (−14)             ──[rule 0 eval_add @ site 0 (144 + (−14))]──►  130

result 130
       solved in 4 step(s), par 3, z = −1
```

<sub>start tokens: `6 1 4 1 39 2 7 1 4 1 41 2 4 1 62 2 2 8 1 4 1 44 2 4 1 44 2 2 2`</sub>

### 20. evaluate — negative product

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    1  (par_source=bfs)
start  9 × (−7)

9 × (−7)  ──[rule 2 eval_mul @ site 0 (9 × (−7))]──►  −63

result −63
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `8 1 4 1 41 2 5 1 39 2 2`</sub>

### 21. simplify — like terms

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    1  (par_source=bfs)
start  2x + 3x

2x + 3x  ──[rule 3 combine_like_terms @ site 0 (2x + 3x)]──►  5x

result 5x
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `6 1 8 1 4 1 34 2 11 2 8 1 4 1 35 2 11 2 2`</sub>

### 22. simplify — like terms cancel to zero

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    1  (par_source=bfs)
start  3x + (−3x)

3x + (−3x)  ──[rule 3 combine_like_terms @ site 0 (3x + (−3x))]──►  0

result 0
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `6 1 8 1 4 1 35 2 11 2 8 1 5 1 35 2 11 2 2`</sub>

### 23. simplify — coefficient falls to 1

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    1  (par_source=bfs)
start  4x + (−3x)

4x + (−3x)  ──[rule 3 combine_like_terms @ site 0 (4x + (−3x))]──►  x

result x
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `6 1 8 1 4 1 36 2 11 2 8 1 5 1 35 2 11 2 2`</sub>

### 24. simplify — terms and constants

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    2  (par_source=bfs)
start  2x + 3x + 4 + (−9)

2x + 3x + 4 + (−9)  ──[rule 0 eval_add @ site 0 (2x + 3x + 4 + (−9))]──►  2x + 3x + (−5)
2x + 3x + (−5)      ──[rule 3 combine_like_terms @ site 0 (2x + 3x + (−5))]──►  5x + (−5)

result 5x + (−5)
       solved in 2 step(s), par 2, z = 0
```

<sub>start tokens: `6 1 8 1 4 1 34 2 11 2 8 1 4 1 35 2 11 2 4 1 36 2 5 1 41 2 2`</sub>

### 25. simplify — two variables stay apart

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    1  (par_source=bfs)
start  2y + 3x + 4x

2y + 3x + 4x  ──[rule 3 combine_like_terms @ site 0 (2y + 3x + 4x)]──►  2y + 7x

result 2y + 7x
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `6 1 8 1 4 1 34 2 12 2 8 1 4 1 35 2 11 2 8 1 4 1 36 2 11 2 2`</sub>

### 26. simplify — bare x doubles

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    1  (par_source=bfs)
start  x + x + 1

x + x + 1  ──[rule 3 combine_like_terms @ site 0 (x + x + 1)]──►  2x + 1

result 2x + 1
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `6 1 11 11 4 1 33 2 2`</sub>

### 27. simplify — multi-digit coefficients

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    1  (par_source=bfs)
start  700x + 950x

700x + 950x  ──[rule 3 combine_like_terms @ site 0 (700x + 950x)]──►  1650x

result 1650x
       solved in 1 step(s), par 1, z = 0
```

base-625 panel for the largest numeral in this derivation:

```
glyph convention: reckoner-local placeholder, NOT base-625-canonical (vendoring registered as ROUND-03)
950 = D1 D325
┌───┐ ┌───┐
│0 0│ │2 3│
│0 1│ │0 0│
└───┘ └───┘
```

<sub>start tokens: `6 1 8 1 4 1 33 107 2 11 2 8 1 4 1 33 357 2 11 2 2`</sub>

### 28. simplify — a product of numerals inside a sum

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    1  (par_source=bfs)
start  x + 6 × 7

x + 6 × 7  ──[rule 2 eval_mul @ site 2 (6 × 7)]──►  x + 42

result x + 42
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `6 1 11 8 1 4 1 38 2 4 1 39 2 2 2`</sub>

### 29. ILLUSTRATIVE — add_both_sides then recover

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    1  (par_source=bfs)
start  3x = 15

3x = 15            ──[rule 4 add_both_sides @ site 1 (3x)]──►  3x + 15 = 3x + 3x
3x + 15 = 3x + 3x  ──[rule 3 combine_like_terms @ site 6 (3x + 3x)]──►  3x + 15 = 6x
3x + 15 = 6x       ──[rule 5 sub_both_sides @ site 2 (3x)]──►  6x + (−3x) = 15
6x + (−3x) = 15    ──[rule 3 combine_like_terms @ site 1 (6x + (−3x))]──►  3x = 15
3x = 15            ──[rule 6 div_both_sides @ site 2 (3)]──►  x = 5

result x = 5
       solved in 5 step(s), par 1, z = −1
```

<sub>start tokens: `10 1 8 1 4 1 35 2 11 2 4 1 47 2 2`</sub>

### 30. ILLUSTRATIVE — add_both_sides on a constant

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  2x + 5 = 17

2x + 5 = 17          ──[rule 4 add_both_sides @ site 5 (5)]──►  2x + 5 + 5 = 5 + 17
2x + 5 + 5 = 5 + 17  ──[rule 0 eval_add @ site 1 (2x + 5 + 5)]──►  2x + 10 = 5 + 17
2x + 10 = 5 + 17     ──[rule 0 eval_add @ site 6 (5 + 17)]──►  2x + 10 = 22
2x + 10 = 22         ──[rule 5 sub_both_sides @ site 5 (10)]──►  2x = 22 + (−10)
2x = 22 + (−10)      ──[rule 0 eval_add @ site 4 (22 + (−10))]──►  2x = 12
2x = 12              ──[rule 6 div_both_sides @ site 2 (2)]──►  x = 6

result x = 6
       solved in 6 step(s), par 3, z = −1
```

<sub>start tokens: `10 1 6 1 8 1 4 1 34 2 11 2 4 1 37 2 2 4 1 49 2 2`</sub>

### 31. ILLUSTRATIVE — add_both_sides with negatives

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  3x + (−9) = 3

3x + (−9) = 3                ──[rule 4 add_both_sides @ site 5 (−9)]──►  3x + (−9) + (−9) = 3 + (−9)
3x + (−9) + (−9) = 3 + (−9)  ──[rule 0 eval_add @ site 1 (3x + (−9) + (−9))]──►  3x + (−18) = 3 + (−9)
3x + (−18) = 3 + (−9)        ──[rule 0 eval_add @ site 6 (3 + (−9))]──►  3x + (−18) = −6
3x + (−18) = −6              ──[rule 5 sub_both_sides @ site 5 (−18)]──►  3x = 18 + (−6)
3x = 18 + (−6)               ──[rule 0 eval_add @ site 4 (18 + (−6))]──►  3x = 12
3x = 12                      ──[rule 6 div_both_sides @ site 2 (3)]──►  x = 4

result x = 4
       solved in 6 step(s), par 3, z = −1
```

<sub>start tokens: `10 1 6 1 8 1 4 1 35 2 11 2 5 1 41 2 2 4 1 35 2 2`</sub>

### 32. evaluate — spread 31

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    1  (par_source=bfs)
start  598 + (−204)

598 + (−204)  ──[rule 0 eval_add @ site 0 (598 + (−204))]──►  394

result 394
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `6 1 4 1 630 2 5 1 236 2 2`</sub>

### 33. simplify — spread 32

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    2  (par_source=bfs)
start  8x + 19x + 19

8x + 19x + 19  ──[rule 3 combine_like_terms @ site 0 (8x + 19x + 19)]──►  27x + 19

result 27x + 19
       solved in 1 step(s), par 2, z = +1
```

<sub>start tokens: `6 1 8 1 4 1 40 2 11 2 8 1 4 1 51 2 11 2 4 1 51 2 2`</sub>

### 34. solve — spread 33

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  9x + (−28) = 44

9x + (−28) = 44  ──[rule 5 sub_both_sides @ site 5 (−28)]──►  9x = 28 + 44
9x = 28 + 44     ──[rule 0 eval_add @ site 4 (28 + 44)]──►  9x = 72
9x = 72          ──[rule 6 div_both_sides @ site 2 (9)]──►  x = 8

result x = 8
       solved in 3 step(s), par 3, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 4 1 41 2 11 2 5 1 60 2 2 4 1 76 2 2`</sub>

### 35. evaluate — spread 34

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    1  (par_source=bfs)
start  825 + (−160)

825 + (−160)  ──[rule 0 eval_add @ site 0 (825 + (−160))]──►  665

result 665
       solved in 1 step(s), par 1, z = 0
```

base-625 panel for the largest numeral in this derivation:

```
glyph convention: reckoner-local placeholder, NOT base-625-canonical (vendoring registered as ROUND-03)
825 = D1 D200
┌───┐ ┌───┐
│0 0│ │1 3│
│0 1│ │0 0│
└───┘ └───┘
```

<sub>start tokens: `6 1 4 1 33 232 2 5 1 192 2 2`</sub>

### 36. simplify — spread 35

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    2  (par_source=bfs)
start  2x + 21x + 2

2x + 21x + 2  ──[rule 3 combine_like_terms @ site 0 (2x + 21x + 2)]──►  23x + 2

result 23x + 2
       solved in 1 step(s), par 2, z = +1
```

<sub>start tokens: `6 1 8 1 4 1 34 2 11 2 8 1 4 1 53 2 11 2 4 1 34 2 2`</sub>

### 37. solve — spread 36

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  −8x + 7 = −73

−8x + 7 = −73     ──[rule 5 sub_both_sides @ site 5 (7)]──►  −8x = −7 + (−73)
−8x = −7 + (−73)  ──[rule 0 eval_add @ site 4 (−7 + (−73))]──►  −8x = −80
−8x = −80         ──[rule 6 div_both_sides @ site 2 (−8)]──►  x = 10

result x = 10
       solved in 3 step(s), par 3, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 5 1 40 2 11 2 4 1 39 2 2 5 1 105 2 2`</sub>

### 38. evaluate — spread 37

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    1  (par_source=bfs)
start  525 + (−14)

525 + (−14)  ──[rule 0 eval_add @ site 0 (525 + (−14))]──►  511

result 511
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `6 1 4 1 557 2 5 1 46 2 2`</sub>

### 39. simplify — spread 38

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    2  (par_source=bfs)
start  3x + (−23x) + 3

3x + (−23x) + 3  ──[rule 3 combine_like_terms @ site 0 (3x + (−23x) + 3)]──►  −20x + 3

result −20x + 3
       solved in 1 step(s), par 2, z = +1
```

<sub>start tokens: `6 1 8 1 4 1 35 2 11 2 8 1 5 1 55 2 11 2 4 1 35 2 2`</sub>

### 40. solve — spread 39

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  −6x + 29 = 95

−6x + 29 = 95     ──[rule 5 sub_both_sides @ site 5 (29)]──►  −6x = 95 + (−29)
−6x = 95 + (−29)  ──[rule 0 eval_add @ site 4 (95 + (−29))]──►  −6x = 66
−6x = 66          ──[rule 6 div_both_sides @ site 2 (−6)]──►  x = −11

result x = −11
       solved in 3 step(s), par 3, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 5 1 38 2 11 2 4 1 61 2 2 4 1 127 2 2`</sub>

### 41. evaluate — spread 40

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    1  (par_source=bfs)
start  −820 + (−372)

−820 + (−372)  ──[rule 0 eval_add @ site 0 (−820 + (−372))]──►  −1192

result −1192
       solved in 1 step(s), par 1, z = 0
```

base-625 panel for the largest numeral in this derivation:

```
glyph convention: reckoner-local placeholder, NOT base-625-canonical (vendoring registered as ROUND-03)
−820 = D1 D195
┌───┐ ┌───┐
│0 0│ │1 2│
│0 1│ │4 0│
└───┘ └───┘
```

<sub>start tokens: `6 1 5 1 33 227 2 5 1 404 2 2`</sub>

### 42. simplify — spread 41

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    2  (par_source=bfs)
start  −18x + (−22x) + (−18)

−18x + (−22x) + (−18)  ──[rule 3 combine_like_terms @ site 0 (−18x + (−22x) + (−18))]──►  −40x + (−18)

result −40x + (−18)
       solved in 1 step(s), par 2, z = +1
```

<sub>start tokens: `6 1 8 1 5 1 50 2 11 2 8 1 5 1 54 2 11 2 5 1 50 2 2`</sub>

### 43. solve — spread 42

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  −9x + (−19) = 53

−9x + (−19) = 53  ──[rule 5 sub_both_sides @ site 5 (−19)]──►  −9x = 19 + 53
−9x = 19 + 53     ──[rule 0 eval_add @ site 4 (19 + 53)]──►  −9x = 72
−9x = 72          ──[rule 6 div_both_sides @ site 2 (−9)]──►  x = −8

result x = −8
       solved in 3 step(s), par 3, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 5 1 41 2 11 2 5 1 51 2 2 4 1 85 2 2`</sub>

### 44. evaluate — spread 43

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    1  (par_source=bfs)
start  406 + 517

406 + 517  ──[rule 0 eval_add @ site 0 (406 + 517)]──►  923

result 923
       solved in 1 step(s), par 1, z = 0
```

base-625 panel for the largest numeral in this derivation:

```
glyph convention: reckoner-local placeholder, NOT base-625-canonical (vendoring registered as ROUND-03)
923 = D1 D298
┌───┐ ┌───┐
│0 0│ │2 1│
│0 1│ │4 3│
└───┘ └───┘
```

<sub>start tokens: `6 1 4 1 438 2 4 1 549 2 2`</sub>

### 45. simplify — spread 44

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    2  (par_source=bfs)
start  2x + 11x + 11

2x + 11x + 11  ──[rule 3 combine_like_terms @ site 0 (2x + 11x + 11)]──►  13x + 11

result 13x + 11
       solved in 1 step(s), par 2, z = +1
```

<sub>start tokens: `6 1 8 1 4 1 34 2 11 2 8 1 4 1 43 2 11 2 4 1 43 2 2`</sub>

### 46. solve — spread 45

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  −3x + (−6) = −3

−3x + (−6) = −3  ──[rule 5 sub_both_sides @ site 5 (−6)]──►  −3x = 6 + (−3)
−3x = 6 + (−3)   ──[rule 0 eval_add @ site 4 (6 + (−3))]──►  −3x = 3
−3x = 3          ──[rule 6 div_both_sides @ site 2 (−3)]──►  x = −1

result x = −1
       solved in 3 step(s), par 3, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 5 1 35 2 11 2 5 1 38 2 2 5 1 35 2 2`</sub>

### 47. evaluate — spread 46

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    1  (par_source=bfs)
start  154 + 346

154 + 346  ──[rule 0 eval_add @ site 0 (154 + 346)]──►  500

result 500
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `6 1 4 1 186 2 4 1 378 2 2`</sub>

### 48. simplify — spread 47

```
ruleset_version=1  vocab_version=1
goal   SIMPLIFY
par    2  (par_source=bfs)
start  9x + 30x + 9

9x + 30x + 9  ──[rule 3 combine_like_terms @ site 0 (9x + 30x + 9)]──►  39x + 9

result 39x + 9
       solved in 1 step(s), par 2, z = +1
```

<sub>start tokens: `6 1 8 1 4 1 41 2 11 2 8 1 4 1 62 2 11 2 4 1 41 2 2`</sub>

### 49. solve — spread 48

```
ruleset_version=1  vocab_version=1
goal   SOLVE for x
par    3  (par_source=bfs)
start  −8x + 22 = −34

−8x + 22 = −34     ──[rule 5 sub_both_sides @ site 5 (22)]──►  −8x = −22 + (−34)
−8x = −22 + (−34)  ──[rule 0 eval_add @ site 4 (−22 + (−34))]──►  −8x = −56
−8x = −56          ──[rule 6 div_both_sides @ site 2 (−8)]──►  x = 7

result x = 7
       solved in 3 step(s), par 3, z = 0
```

<sub>start tokens: `10 1 6 1 8 1 5 1 40 2 11 2 4 1 54 2 2 5 1 66 2 2`</sub>

### 50. evaluate — spread 49

```
ruleset_version=1  vocab_version=1
goal   EVALUATE
par    1  (par_source=bfs)
start  140 + (−146)

140 + (−146)  ──[rule 0 eval_add @ site 0 (140 + (−146))]──►  −6

result −6
       solved in 1 step(s), par 1, z = 0
```

<sub>start tokens: `6 1 4 1 172 2 5 1 178 2 2`</sub>

